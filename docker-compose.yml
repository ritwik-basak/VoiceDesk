services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: ./backend/.env
    restart: always

  agent:
    build: ./backend
    command: python agent_worker.py
    env_file: ./backend/.env
    restart: always
    depends_on:
      - api
